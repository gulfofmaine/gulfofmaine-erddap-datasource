apiVersion: 1

datasources:
  - name: 'erddap'
    # An explicit uid so provisioned alert rules can reference this datasource.
    uid: 'erddap'
    type: 'gulfofmaine-erddap-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      baseUrl: 'https://data.neracoos.org/erddap'
